opentp: 2025-06

event:
  key: dashboard::dashboard_view

  lifecycle:
    status: active

  taxonomy:
    area: dashboard
    event: dashboard_view
    action: User opens the dashboard

  payload:
    platforms:
      all:
        active: 1.0.0
        history:
          1.0.0:
            changes: []
            schema:
              application_id:
                value: web-app
              event_name:
                value: dashboard_view
              event_category:
                value: dashboard
              widgets_count:
                type: number
                required: false
              has_notifications:
                type: boolean
                required: false
